default:
    @just --list

# Install project dependencies via uv sync.
setup:
    uv sync

# Run the langgraph-claude-agents CLI against the given GitHub issue number.
run issue:
    uv run langgraph-claude-agents --issue {{issue}}
